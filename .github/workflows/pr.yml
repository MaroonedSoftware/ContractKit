name: Pull Request
on:
  pull_request:

jobs:
  pull-request:
    uses: ./.github/workflows/build.yml
    # No `secrets: inherit`: build.yml uses no secrets, and this workflow runs
    # untrusted PR code (install + build:ci + test:ci). Do not forward repo
    # secrets down this path.
