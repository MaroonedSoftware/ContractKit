// Auto-generated by @contractkit/plugin-kotlin. Do not edit manually.
@file:OptIn(ExperimentalUuidApi::class)

package com.example.sdk.models

import com.example.sdk.runtime.BigInt
import com.example.sdk.runtime.Decimal
import com.example.sdk.runtime.PairAsArraySerializer
import com.example.sdk.runtime.TripleAsArraySerializer
import kotlin.time.Duration
import kotlin.time.Instant
import kotlin.uuid.ExperimentalUuidApi
import kotlin.uuid.Uuid
import kotlinx.datetime.LocalDate
import kotlinx.datetime.LocalTime
import kotlinx.serialization.KSerializer
import kotlinx.serialization.SerialName
import kotlinx.serialization.Serializable
import kotlinx.serialization.SerializationException
import kotlinx.serialization.builtins.serializer
import kotlinx.serialization.descriptors.SerialDescriptor
import kotlinx.serialization.descriptors.buildClassSerialDescriptor
import kotlinx.serialization.encoding.Decoder
import kotlinx.serialization.encoding.Encoder
import kotlinx.serialization.json.JsonDecoder
import kotlinx.serialization.json.JsonElement
import kotlinx.serialization.json.JsonEncoder
import kotlinx.serialization.json.contentOrNull
import kotlinx.serialization.json.decodeFromJsonElement
import kotlinx.serialization.json.encodeToJsonElement
import kotlinx.serialization.json.jsonObject
import kotlinx.serialization.json.jsonPrimitive

/** A named enum, so a field default has to resolve to a member rather than its wire spelling */
@Serializable
enum class Rating {
    @SerialName("good")
    GOOD,
    @SerialName("neutral")
    NEUTRAL,
    @SerialName("bad")
    BAD,
}

@Serializable
data class Card(
    val kind: String = "card",
    val last4: String,
) : Instrument

@Serializable
data class Bank(
    val kind: String = "bank",
    val iban: String,
) : Instrument

/** Decodes snake_case keys and encodes PascalCase ones */
@Serializable
data class Token(
    @SerialName("access_token") val accessToken: String,
    @SerialName("expires_in") val expiresIn: Long? = 3600L,
)

@Serializable
data class Owned(
    val id: Uuid,
)

@Serializable
data class OwnedInput(
    val secret: String,
)

@Serializable
data class Named(
    val name: String,
)

/** Two flattened bases, split into a read and an input shape */
@Serializable
data class Shared(
    val id: Uuid,
    val name: String,
    val label: String? = "x",
    val instrument: Instrument? = null,
)

/** Two flattened bases, split into a read and an input shape */
@Serializable
data class SharedInput(
    val secret: String,
    val name: String,
    val label: String? = "x",
    val instrument: Instrument? = null,
)

/**
 * Self recursion through lazy(), mutual recursion through Doc, every container, both union
 * forms, every scalar, and field names that are keywords in the target languages
 */
@Serializable
data class Folder(
    val id: Uuid,
    val `class`: String,
    val default: String? = null,
    val rating: Rating? = Rating.NEUTRAL,
    val parent: Folder? = null,
    val readme: Doc? = null,
    val children: List<Folder>,
    val byName: Map<String, Folder>? = null,
    @Serializable(with = PairAsArraySerializer::class) val span: Pair<Long, Long>? = null,
    @Serializable(with = TripleAsArraySerializer::class) val stamped: Triple<Uuid, Instant, Boolean>? = null,
    val label: FolderLabel,
    val pinned: FolderPinned?,
    val instrument: FolderInstrument? = null,
    val origin: FolderOrigin? = null,
    val size: BigInt,
    val price: Decimal,
    val day: LocalDate? = null,
    val at: LocalTime? = null,
    val ttl: Duration? = null,
    val blob: ByteArray? = null,
    val extra: JsonElement? = null,
    val raw: JsonElement? = null,
)

@Serializable
data class Doc(
    val id: Uuid,
    val folder: Folder? = null,
)

@Serializable(with = FolderLabelSerializer::class)
sealed interface FolderLabel {
    data class OfString(val value: String) : FolderLabel
    data class OfInt(val value: Long) : FolderLabel
}

object FolderLabelSerializer : KSerializer<FolderLabel> {
    override val descriptor: SerialDescriptor = buildClassSerialDescriptor("FolderLabel")

    override fun serialize(encoder: Encoder, value: FolderLabel) {
        val output = encoder as? JsonEncoder ?: throw SerializationException("FolderLabel can only be encoded as JSON")
        val element = when (value) {
            is FolderLabel.OfString -> output.json.encodeToJsonElement(String.serializer(), value.value)
            is FolderLabel.OfInt -> output.json.encodeToJsonElement(Long.serializer(), value.value)
        }
        output.encodeJsonElement(element)
    }

    override fun deserialize(decoder: Decoder): FolderLabel {
        val input = decoder as? JsonDecoder ?: throw SerializationException("FolderLabel can only be decoded from JSON")
        val element = input.decodeJsonElement()
        runCatching { FolderLabel.OfString(input.json.decodeFromJsonElement(String.serializer(), element)) }.getOrNull()?.let { return it }
        runCatching { FolderLabel.OfInt(input.json.decodeFromJsonElement(Long.serializer(), element)) }.getOrNull()?.let { return it }
        throw SerializationException("No FolderLabel member matched the payload")
    }
}

@Serializable(with = FolderPinnedSerializer::class)
sealed interface FolderPinned {
    data class OfDoc(val value: Doc) : FolderPinned
    data class OfFolder(val value: Folder) : FolderPinned
}

object FolderPinnedSerializer : KSerializer<FolderPinned> {
    override val descriptor: SerialDescriptor = buildClassSerialDescriptor("FolderPinned")

    override fun serialize(encoder: Encoder, value: FolderPinned) {
        val output = encoder as? JsonEncoder ?: throw SerializationException("FolderPinned can only be encoded as JSON")
        val element = when (value) {
            is FolderPinned.OfDoc -> output.json.encodeToJsonElement(Doc.serializer(), value.value)
            is FolderPinned.OfFolder -> output.json.encodeToJsonElement(Folder.serializer(), value.value)
        }
        output.encodeJsonElement(element)
    }

    override fun deserialize(decoder: Decoder): FolderPinned {
        val input = decoder as? JsonDecoder ?: throw SerializationException("FolderPinned can only be decoded from JSON")
        val element = input.decodeJsonElement()
        runCatching { FolderPinned.OfDoc(input.json.decodeFromJsonElement(Doc.serializer(), element)) }.getOrNull()?.let { return it }
        runCatching { FolderPinned.OfFolder(input.json.decodeFromJsonElement(Folder.serializer(), element)) }.getOrNull()?.let { return it }
        throw SerializationException("No FolderPinned member matched the payload")
    }
}

@Serializable(with = FolderInstrumentSerializer::class)
sealed interface FolderInstrument {
    data class OfCard(val value: Card) : FolderInstrument
    data class OfBank(val value: Bank) : FolderInstrument
}

object FolderInstrumentSerializer : KSerializer<FolderInstrument> {
    override val descriptor: SerialDescriptor = buildClassSerialDescriptor("FolderInstrument")

    override fun serialize(encoder: Encoder, value: FolderInstrument) {
        val output = encoder as? JsonEncoder ?: throw SerializationException("FolderInstrument can only be encoded as JSON")
        val element = when (value) {
            is FolderInstrument.OfCard -> output.json.encodeToJsonElement(Card.serializer(), value.value)
            is FolderInstrument.OfBank -> output.json.encodeToJsonElement(Bank.serializer(), value.value)
        }
        output.encodeJsonElement(element)
    }

    override fun deserialize(decoder: Decoder): FolderInstrument {
        val input = decoder as? JsonDecoder ?: throw SerializationException("FolderInstrument can only be decoded from JSON")
        val element = input.decodeJsonElement()
        runCatching { FolderInstrument.OfCard(input.json.decodeFromJsonElement(Card.serializer(), element)) }.getOrNull()?.let { return it }
        runCatching { FolderInstrument.OfBank(input.json.decodeFromJsonElement(Bank.serializer(), element)) }.getOrNull()?.let { return it }
        throw SerializationException("No FolderInstrument member matched the payload")
    }
}

@Serializable
data class FolderOrigin(
    val x: Double,
    val y: Double,
)

@Serializable(with = InstrumentSerializer::class)
sealed interface Instrument

object InstrumentSerializer : KSerializer<Instrument> {
    override val descriptor: SerialDescriptor = buildClassSerialDescriptor("Instrument")

    override fun serialize(encoder: Encoder, value: Instrument) {
        val output = encoder as? JsonEncoder ?: throw SerializationException("Instrument can only be encoded as JSON")
        val element = when (value) {
            is Card -> output.json.encodeToJsonElement(Card.serializer(), value)
            is Bank -> output.json.encodeToJsonElement(Bank.serializer(), value)
        }
        output.encodeJsonElement(element)
    }

    override fun deserialize(decoder: Decoder): Instrument {
        val input = decoder as? JsonDecoder ?: throw SerializationException("Instrument can only be decoded from JSON")
        val element = input.decodeJsonElement()
        return when (val tag = element.jsonObject["kind"]?.jsonPrimitive?.contentOrNull) {
            "card" -> input.json.decodeFromJsonElement(Card.serializer(), element)
            "bank" -> input.json.decodeFromJsonElement(Bank.serializer(), element)
            else -> throw SerializationException("Unknown Instrument kind: $tag")
        }
    }
}
