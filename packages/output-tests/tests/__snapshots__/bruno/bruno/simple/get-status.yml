info:
  name: /status
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/status"

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"

docs: |-
  current service status
