info:
  name: /ledgers
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/ledgers"
  body:
    type: json
    data: |
      {
        "entryCode": "",
        "postedAt": "2024-01-01T00:00:00Z"
      }

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "201"
