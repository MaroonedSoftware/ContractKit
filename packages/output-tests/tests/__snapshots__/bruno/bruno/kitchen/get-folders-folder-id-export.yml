info:
  name: "/folders/{folder-id}/export"
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/folders/:folderId/export"
  params:
    - name: folderId
      value: "00000000-0000-0000-0000-000000000000"
      type: path

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"
    - expression: res.headers["x-export-id"]
      operator: isDefined
      value: ""

docs: |-
  one status with two content types and response headers, so the headers are read before the mime dispatch
  
  **Response headers**
  
  - `x-export-id` (required)
  - `x-rows` (optional)
