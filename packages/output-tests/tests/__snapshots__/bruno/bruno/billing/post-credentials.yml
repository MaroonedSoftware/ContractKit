info:
  name: /credentials
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/credentials"
  body:
    type: json
    data: |
      {
        "secret": "",
        "scope": "",
        "token": ""
      }

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"

docs: |-
  store a credential
