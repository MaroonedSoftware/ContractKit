info:
  name: "/refunds/{paymentId}"
  type: http
  seq: 8

http:
  method: GET
  url: "{{baseUrl}}/refunds/:paymentId"
  params:
    - name: paymentId
      value: "00000000-0000-0000-0000-000000000000"
      type: path

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"

docs: |-
  look up a refund by its originating payment
