info:
  name: /sessions
  type: http
  seq: 9

http:
  method: POST
  url: "{{baseUrl}}/sessions"
  body:
    type: json
    data: |
      {
        "id": "",
        "refreshToken": ""
      }

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"

docs: |-
  open a session
