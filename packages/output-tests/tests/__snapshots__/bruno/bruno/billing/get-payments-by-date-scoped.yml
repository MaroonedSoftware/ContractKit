info:
  name: /payments/by-date/scoped
  type: http
  seq: 10

http:
  method: GET
  url: "{{baseUrl}}/payments/by-date/scoped"

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"

docs: |-
  search payments with a snake_case filter extended inline
