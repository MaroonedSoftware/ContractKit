info:
  name: "/seats/{seatId}"
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/seats/:seatId"
  params:
    - name: seatId
      value: ""
      type: path
    - name: from
      value: ""
      type: query
      disabled: true
    - name: pageSize
      value: "0"
      type: query
      disabled: true

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"

docs: |-
  fetch one seat
  
  **Response headers**
  
  - `from` (optional)
