info:
  name: "/invoices/{invoice-id}"
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/invoices/{invoice-id}"

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"

docs: |-
  fetch an invoice
