---
title: "One status with two content types and response headers, so the headers are read before the mime dispatch"
sidebarTitle: "One status with two content types and response headers, so the headers are read before the mime dispatch"
openapi: "/openapi.yaml GET /folders/{folder-id}/export"
---
